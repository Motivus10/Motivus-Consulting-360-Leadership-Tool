[phases.setup]
nixPkgs = ["nodejs_18", "python3", "gcc", "gnumake"]

[phases.install]
cmds = ["cd server && npm install"]

[phases.build]
cmds = ["echo ready"]

[start]
cmd = "cd server && node index.js"
